//////partA


1- Explain, in code comments at the top of Program.cs, the role of: .csproj, Program.cs, obj/, and bin/.

 .csproj: Defines the project configuration, target framework, packages, and build settings.

 Program.cs: The main source file where the application code starts and runs.

 bin/: Contains the final compiled output of the project, such as the executable and (DLL ,exe) files.

 obj/: Contains temporary files and intermediate build information generated by .NET.

 2- <Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>

 3-Add a file-scoped namespace to Program.cs and explain in a comment why it removes a level of indentation.
 beacuse  i  removes the need for curly braces

 4-  This project uses  .slnx format.
 .sln format is in older  Visual Studio versions.







 //////partG


2- Do #region / #endregion change the compiled output? Why might you still use them?
 do not change the compiled output. They are used to organize code and make it easier.

 3-When would you reach for /// XML doc comments instead of a plain //?
 when i need to tell anther devolopers know how to using my function or class or do code production 


 4- Why does C# have no true global variables, and what's the closest equivalent?
 because the c# is object -oriented programming language so it variable store in scope of class or method .
 closest equivalent: using keyword static (create a static class or static  varible)